---
name: project-helpdesk-auth-e2e
description: Helpdesk app's e2e auth test coverage (login/logout/session) and the concrete facts needed to write/extend it correctly
metadata:
  type: project
---

As of 2026-06-20, `e2e/login.spec.ts`, `e2e/logout.spec.ts`, and `e2e/session.spec.ts` together give comprehensive coverage of the authentication system (login validation, failed sign-in, submitting state, logout, session persistence, unauthenticated deep-link). `e2e/authorization.spec.ts` covers role-based access separately (non-admin blocked from `/user`) — don't duplicate that ground.

## Concrete facts confirmed by hitting the real endpoint (not guessed)
- better-auth's generic sign-in failure message, verified via `curl -X POST http://localhost:3002/api/auth/sign-in/email` against the running test server, is **exactly** `"Invalid email or password"` (code `INVALID_EMAIL_OR_PASSWORD`) — identical for wrong-password and non-existent-email cases. This confirms there's no UI-visible enumeration leak. Use `page.getByText(/invalid email or password/i)` to assert it.
- The error/field-error component (`client/src/components/ui/field.tsx` `FieldError`) renders a `<div role="alert" data-slot="field-error">` — `page.getByRole('alert')` is sufficient; no need to also match `[data-slot="field-error"]`, they're the same element.
- `Login.tsx`'s submit button text swaps to exactly `"Signing in…"` (with the real ellipsis character `…`, not three periods) while `isSubmitting` is true, and is `disabled` in that state. To assert this without flakiness, intercept `**/api/auth/sign-in/email` with `page.route` and inject an artificial delay (e.g. 300ms via `setTimeout` then `route.continue()`) so the submitting state has time to render before resolving.
- Zod schema in `Login.tsx`: empty email → `"Email is required"`; malformed email → `"Enter a valid email"`; empty password → `"Password is required"`. These are two distinct messages for email (required vs format), not one shared "invalid" message.
- To verify no network call fires for pure client-side validation failures (e.g. empty email), use `page.route('**/api/auth/sign-in/email', ...)` with a flag set inside the handler, then assert the flag is still `false` after submit — cheap and avoids relying on timing.

## Test layout decision (and why)
- Extended `e2e/login.spec.ts` in place (rather than a separate `auth-edge-cases.spec.ts`) with two `test.describe` blocks: "client-side field validation" (empty email/password, malformed email) and "failed sign-in attempts" (wrong password, non-existent email, identical-error-text check, retry-after-failure flow), plus one top-level test for the "Signing in…" submitting state. Rationale: all of these are variations on "what happens when you try to log in" — same file's existing scope, just deepened. Kept `logout.spec.ts` and `authorization.spec.ts` untouched since they're already tightly scoped.
- Added a new `e2e/session.spec.ts` for cross-cutting session concerns that don't belong to "login" or "logout" specifically: session survives a reload while authenticated (inverse of what `logout.spec.ts` checks), and an unauthenticated user hitting a deep link (`/user`) gets the login form via `App.tsx`'s `useSession()` gate rather than a crash/blank page.
- Did NOT add: a UI signup test (no signup UI exists, `disableSignUp: true` in `server/src/auth.ts`), a 429 rate-limit test (disabled outside `NODE_ENV=production`, e2e runs with rate limiting off by design — see [[project-helpdesk-auth]] memory in the security-reviewer namespace for the history here), or an unverified-email-account test (would require extending `server/prisma/seed.e2e.ts`, which is a shared fixture other specs may depend on — flagged to the user as a suggestion rather than just doing it).

## Related: general UI flow coverage (e2e/ui.spec.ts)
As of 2026-06-21, added `e2e/ui.spec.ts` for non-auth UI flows: admin clicking "Users" nav link lands on `/user` (the inverse of what `authorization.spec.ts` checks for AGENT), the Home page health badge (`Badge` text is exactly `API: {health}`, e.g. `API: ok` — `/api/health` is unauthenticated and always returns `{status:"ok"}` in this env so this is deterministic, not flaky), empty/whitespace-only ticket subject submission firing no POST (same `page.route` flag-interception pattern as `login.spec.ts`'s empty-email test), and the Tickets card's `{n} total` count incrementing by exactly 1 after creating a ticket (relative assertion via regex `/total$/` text match — read count before, create, assert `beforeCount + 1`).

Deliberately skipped: the "No tickets yet." empty-state text, because `helpdesk_test`'s Tickets table has accumulated rows across many prior e2e runs and there's no DELETE endpoint in `server/src/index.ts` (only GET/POST `/api/tickets`) to clear it. Flagged back to the user rather than truncating the table directly via Prisma, since that's shared fixture data other specs may rely on incidentally. If a reset mechanism is ever added (e.g. a test-only truncate endpoint or a `db:test:reset` script), this gap is worth revisiting.

## Fixed flake in e2e/ui.spec.ts ticket-count assertions (2026-06-21)
All three "ticket form" tests captured the "before" `{n} total` count via a one-shot `.textContent()` call placed right after `login()` returned. `Home.tsx`'s `tickets` state starts as `[]` and `GET /api/tickets` resolves asynchronously, so there's a real (if usually narrow) window where `{tickets.length} total` renders `"0 total"` before the fetch resolves and re-renders with the true count. Whichever test's `.textContent()` call happened to land in that window captured `"0 total"` as its baseline and then failed once the real count (legitimately non-zero, accumulating across e2e runs since there's no truncate endpoint) loaded a moment later. This is exactly the `expect(locator).toHaveText(expected)` / "Received: 10 total" failure mode — not an app bug.

Fix applied: in each of the three tests, register `const ticketsLoaded = page.waitForResponse(res => res.url().includes('/api/tickets') && res.request().method() === 'GET')` *before* calling `login()`, then `await ticketsLoaded` immediately after `login()` resolves, and only then read the baseline count text/number. This guarantees the baseline is read post-settle, not mid-fetch. Confirmed stable across 3 consecutive full `bun run test:e2e` runs (19/19 each) after the fix. General lesson for this app: any test reading `{n} total` (or anything else driven by a `useEffect` fetch with an empty-array initial state) needs to wait for that fetch's response, not just for `login()`'s "Sign out" button visibility — `login()` only proves auth succeeded, not that `Home`'s own data fetches have resolved.

## Admin-only Users page + API coverage (2026-06-21)
Backend added `GET /api/users` (`requireRole(Role.ADMIN)` in `server/src/middleware.ts`): 401 with `{message:"Unauthorized"}` if no session, 403 with `{message:"Forbidden"}` if session role isn't ADMIN, else 200 with an array of `{id, name, email, role, emailVerified, createdAt}` (explicit Prisma `select`, sorted `name: "asc"`). Frontend `client/src/pages/UsersPage.tsx` is a real `<h1>` (a genuine heading, unlike `CardTitle`/`CardDescription` which are plain divs) backed by a `useEffect` fetch with loading/error/empty states and one `<li>` per user showing name, email, a `Badge variant="destructive"` "unverified" tag when `!emailVerified`, and a role `Badge` (`default` for ADMIN, `secondary` for AGENT).

Both seeded users (`server/prisma/seed.e2e.ts`) are literally named "Admin" and "Agent" with `emailVerified: true` always — confirmed via direct curl against the running test server that `name: "asc"` sorting puts Admin before Agent. This means the "unverified" badge can never be exercised against the two standing seeded accounts (no signup UI/endpoint exists — `disableSignUp: true` in `server/src/auth.ts` — so there's no way to create an unverified user from e2e without writing directly to the DB). Flagged as a gap rather than fabricated; revisit if a test-only "create unverified user" fixture is ever added.

Added two things:
1. Extended `e2e/ui.spec.ts`'s existing placeholder-era "Users" nav test (`navigation` describe block, only checked the heading) to also `waitForResponse` on the `/api/users` GET before asserting, and added a new `users page` describe block with two tests: one confirming both seeded users render with correct name/email/role badge and that admin's row precedes agent's row in DOM order (via `el.parentElement.children` index comparison — avoids hardcoding an exact row count since the table is never truncated and other specs could theoretically add rows later), and one confirming the error state (`page.route` returning a 500) shows "Failed to load users.".
2. New `e2e/users-api.spec.ts` using the `request` fixture directly (not `page`) for endpoint-level authorization proof, independent of the UI: unauthenticated GET → 401, agent-authenticated GET → 403, admin-authenticated GET → 200 with both users and an exact-key-set assertion (`Object.keys(user).sort()`) proving no sensitive fields leak. Authentication for the `request` fixture is done by `request.post('/api/auth/sign-in/email', { data: {...} })` against the relative path — Playwright's `request` fixture shares cookies with `baseURL` (`http://localhost:4173`), and since `/api` is Vite-proxied with `changeOrigin: true` to the server, this avoids needing to hit port 3002 directly or set an `Origin` header manually. This `request`-fixture-with-cookie-sign-in pattern is reusable for any future endpoint-level authorization test in this app.

Gotcha caught by the suite itself: first draft of the exact-key-set assertion in `users-api.spec.ts` omitted `'name'` from the expected array (copy-paste slip) — failed on first run with a clear diff, fixed immediately. Confirmed stable across 3 consecutive full `bun run test:e2e` runs (24/24 each) after the fix.

## UsersPage redesign: <li> list → real shadcn <Table> (2026-06-21)
`client/src/pages/UsersPage.tsx` was redesigned from `<li>`-per-user to a real shadcn `<Table>` (`client/src/components/ui/table.tsx` — plain semantic `<table>/<thead>/<tbody>/<tr>/<th>/<td>`, so native ARIA table roles work for free, no extra `role=` attrs needed). Exactly 4 columns now: Name, Email, Role, Created — in that order. The "unverified" `Badge` that used to render next to `!emailVerified` users (mentioned in the entry above) was removed entirely; the new design has no verification-status indicator at all.

This broke `e2e/ui.spec.ts`'s `users page` describe block, which used `getByRole('listitem')` and asserted `unverified` badge non-visibility. Fixed by switching to `getByRole('row').filter({ hasText: <email> })` for row scoping (note: `getByRole('row')` also matches the header `<tr>`, but filtering by a seeded user's email naturally excludes it since that text only appears in data rows) and `getByRole('cell', { name: 'ADMIN'|'AGENT', exact: true })` for the role-badge cell. Removed the dead "unverified badge not visible" assertions (the feature is gone, not hidden — asserting absence of removed DOM tested nothing) and replaced with a real assertion: `page.getByRole('columnheader')` collected and asserted via `toHaveText(['Name', 'Email', 'Role', 'Created'])` to lock in the exact column set and order. The nav test and the error-state test in the same file needed no changes — both were already markup-agnostic (heading text + response-wait; error text only). Confirmed stable across 2 consecutive full `bun run test:e2e` runs (24/24 each) after the fix.

General lesson: when a project migrates from ad-hoc `<li>`/`<div>` markup to real semantic HTML (tables, native form elements, etc.), prefer the resulting ARIA roles (`row`/`cell`/`columnheader` for tables) over `getByText`/`filter` soup — they're more robust to styling changes and read closer to how a real user/assistive-tech would query the page.

## Add user dialog + POST /api/users coverage (2026-06-24)
Added "create user" feature coverage. Client: `UsersPage.tsx`'s "Add user" button opens a Radix `Dialog` (real `role="dialog"` semantics — `page.getByRole('dialog')` works directly, no extra wiring needed) with a react-hook-form+zod form (Name/Email/Password, labels wired via `FieldLabel htmlFor`, so `page.getByLabel(...)` resolves cleanly even though the login form also has `getByLabel('Email')`/`'Password'` — never ambiguous since login isn't rendered once you're on `/user`). Server: `POST /api/users` (`server/src/index.ts` lines 61-100), same `requireRole(Role.ADMIN)` gate as `GET /api/users`, validates name (min 3) → email (regex) → password (min 8) → 409 duplicate-email, in that exact order, then 201 with the same `userSelect` shape as GET (role always comes out `AGENT`, no role picker exists, `emailVerified: true` hardcoded).

Confirmed `FieldError` (`client/src/components/ui/field.tsx`) renders `role="alert"` in **both** its modes: per-field zod errors (`errors={[...]}` prop) and the standalone server-error usage (`<FieldError>{submitError}</FieldError>`, children mode) — both hit the same component, same role. No need to distinguish them in assertions; `page.getByText(exact message)` is sufficient and was used here in preference to `getByRole('alert')` since multiple alerts could theoretically coexist (not actually exercised, but kept tests robust against it by not assuming only one alert exists at a time).

Test layout: extended `e2e/ui.spec.ts` with a new `add user dialog` describe block (sibling to the existing `users page` block) — 5 tests: happy path (fills form, asserts dialog closes via `expect(dialog).not.toBeVisible()`, new row appears via the same `getByRole('row').filter({hasText: email})` + `getByRole('cell', {name: 'AGENT', exact: true})` pattern as the existing users-page test, and `{n} total` count pattern from the ticket tests — registered the `waitForResponse` for the GET `/api/users` *before* `login()`, same flake-fix as documented above, this time for the Users page's own count display), three client-validation tests (short name/malformed email/short password, each using the `page.route` + boolean-flag-on-POST pattern, asserting the dialog stays open and the exact zod message renders), and one server-conflict test (submits `ADMIN.email`, asserts "A user with this email already exists" plus dialog still open). Extended `e2e/users-api.spec.ts` with a new `POST /api/users authorization` block mirroring the existing `GET /api/users authorization` block exactly: 401 unauthenticated, 403 agent, 201 admin with `toMatchObject` on `{name, email, role: 'AGENT', emailVerified: true}` plus the same exact-key-set assertion pattern.

Dialog-closed assertion that worked cleanly: `await expect(page.getByRole('dialog')).not.toBeVisible()` right after `await page.waitForResponse(...)` on the POST — no extra wait needed beyond that; Radix unmounts/hides synchronously enough with React's state update that there was no observed flake across 2 consecutive full-suite runs (32/32 both times). Confirmed stable, no fixes needed on first write — this is a case worth noting since most prior additions to this suite needed at least one flake-fix iteration.

## Edit user dialog + PATCH /api/users/:id coverage (2026-06-24)
Added "edit user" feature coverage. A 5th `<TableHead>` ("Actions", visually-hidden via `<span className="sr-only">`) was added to the Users table for a per-row icon-only edit button (`aria-label="Edit <name>"`). Fixed the pre-existing column-header assertion in `users page` describe block: `toHaveText(['Name', 'Email', 'Role', 'Created', 'Actions'])` (was missing `'Actions'`) — confirmed this was the *only* failure before starting (17 passed/1 failed baseline).

The edit dialog is a second, independently-controlled Radix `Dialog` (`open={editingUser !== null}`, no `DialogTrigger`) rendered as a sibling to the `Card`, reusing the same field *labels* ("Name"/"Email"/"Password") as the add-user dialog but different element `id`s (`#edit-name`/`#edit-email`/`#edit-password`). Since both dialogs are mounted in the same DOM tree (Radix just toggles visibility), unscoped `page.getByLabel('Name')` etc. is ambiguous once both could theoretically be relevant — but the *actual* failure mode hit in practice was different and worse:

**Real gotcha (caused a real test failure, not hypothetical):** `page.getByLabel('Name')` matches by accessible name on *any* element, not just form controls — including the row-level edit `<button aria-label="Edit <user's name>">`. Once a test created/edited a user whose name contained the substring "Name" (e.g. `Edited Name 123`), that row's button (`aria-label="Edit Edited Name 123"`) became a second match for `getByLabel('Name')` and Playwright's strict mode threw `resolved to 2 elements`. Because `helpdesk_test` is never reset between runs, this row persists forever once created — meaning the very first run that created such a name could break *every later run* of any test using unscoped `page.getByLabel('Name'|'Email'|'Password')`, including the pre-existing `add user dialog` tests. Fixed by: (1) scoping every `getByLabel` call in both the `add user dialog` and `edit user dialog` describe blocks to `dialog.getByLabel(...)` (the open `page.getByRole('dialog')` locator) instead of `page.getByLabel(...)`, and (2) as defense in depth, avoiding the substring "Name" in any test-created user's name going forward (used names like `E2E Edited User ${Date.now()}` instead of anything containing "...Name..."). General lesson for this app: never use unscoped `getByLabel` on a page that also contains `aria-label`-only icon buttons whose label text could overlap with a form field's label word — scope to a dialog/form/row locator first.

Other facts confirmed:
- `updateUserSchema` (`core/src/schemas/user.ts`) makes `password` a plain `z.string().trim().refine(v => v === "" || v.length >= 8, ...)` — i.e. the schema itself trims, so a whitespace-only password client-side input becomes `""` before validation and before the PATCH body is sent. This matches the server's `password === "" ? null : hashPassword(...)` check exactly (`server/src/index.ts` PATCH handler) — no separate test needed for "whitespace-only password", it's equivalent to "empty password" once trimmed.
- Server PATCH conflict check correctly excludes the user's own current email (`emailTaken.id !== userId`) — changing-to-same-email is not a 409. Not separately tested (out of the requested scope) but confirmed by reading the route code.
- `page.request` (as opposed to the bare `request` fixture) shares cookies with whatever `BrowserContext` the `page` belongs to — useful for creating disposable fixtures via the API *while already logged in via the UI* in the same test, without manually copying cookies. The bare `request` fixture from `{ request }` in a test's param list is a separate, cookie-isolated `APIRequestContext` — fine for tests that only ever need their own sign-in (as `users-api.spec.ts` already did), but the wrong tool when a test needs to mix `page`-based UI state with API-level setup. Used `page.request.post('/api/users', ...)` for the happy-path edit test's disposable-user setup specifically because the test was already logged in as ADMIN via `page`.
- `POST /api/auth/sign-out` (better-auth's standard sign-out endpoint) works directly via the `request` fixture to drop a session between sign-ins within the same test — used in the new `users-api.spec.ts` `createDisposableUser` helper to create a fixture as ADMIN then immediately drop that session so the caller can sign in as whichever role it actually wants to test, all within one test function without needing separate `request` fixture instances.

Test layout: extended `e2e/ui.spec.ts` with a new `edit user dialog` describe block (sibling to `add user dialog`) — 6 tests: pre-fill check (name/email populated, password empty), happy path (change name+email, blank password, table updates, then an API-level `request.post('/api/auth/sign-in/email')` proves the ORIGINAL password still authenticates), three client-validation tests (short name/malformed email/short password via the `page.route` + boolean-flag-on-PATCH pattern), one server-conflict test (edits a disposable user's email to collide with `AGENT.email`). Extended `e2e/users-api.spec.ts` with a `PATCH /api/users/:id authorization` block (401/403/200) using a `createDisposableUser` helper that signs in as ADMIN, POSTs a throwaway user, signs out, and returns the new user's `{id, email}` — keeps the seeded ADMIN/AGENT fixtures completely untouched. Confirmed stable across 2 consecutive full `e2e/ui.spec.ts` + `e2e/users-api.spec.ts` runs (27/27 both times) and one full-suite run (all 41 e2e tests, 41/41).

## Delete user (soft delete) coverage + fixing a selector collision (2026-06-24)
A "Delete" icon button (`aria-label="Delete <name>"`) was added per row next to the existing "Edit" button, disabled (`title="Admin users cannot be deleted"`) when `role === 'ADMIN'`. This broke the pre-existing edit-dialog tests: every call site used the loose `getByRole('button', { name: /Edit/ })` regex, and once a *second* button's aria-label also contains the substring "Edit" (e.g. the fixture user named "E2E Edit Target" produces `aria-label="Delete E2E Edit Target"`, which still contains "Edit" as a substring of the *name*, not the action), the regex matches both the Edit and Delete buttons in that row → strict-mode violation. Fixed all 6 call sites across `edit user dialog` tests in `e2e/ui.spec.ts` by switching to exact-string matches on the real accessible name (`{ name: 'Edit Agent', exact: true }`, `{ name: 'Edit E2E Edit Target', exact: true }`, `{ name: 'Edit Conflict Target', exact: true }`) instead of a regex — the accessible name is always exactly `` `Edit ${user.name}` `` / `` `Delete ${user.name}` ``, so `exact: true` with the literal string fully disambiguates with zero ambiguity risk going forward, unlike a substring/regex match which is only safe until some future fixture name happens to overlap. General lesson reinforced: prefer `{ name: '<literal>', exact: true }` over `{ name: /pattern/ }` for any icon-only button whose visible "text" is entirely derived from `aria-label` interpolation — regexes look safe until a second sibling button's label shares a substring.

Delete confirmation uses a shadcn/Radix `AlertDialog` (`role="alertdialog"`), a different component from the `Dialog` (`role="dialog"`) used for add/edit — `page.getByRole('alertdialog')` scopes it correctly and is never ambiguous with the regular dialog since only one of the two dialog types is ever open at a time in these tests. Confirmed empirically (not just assumed) that `alertdialog.getByRole('button', { name: 'Delete', exact: true })` resolves to exactly one element even though the dialog's title text is also "Delete user" — `AlertDialogTitle` renders as a plain div/heading-like element, not a `<button>`, so `getByRole('button', ...)` never considers it a candidate regardless of exact-match strictness. No flake or ambiguity hit in practice across 2 consecutive full-suite runs.

Server: `DELETE /api/users/:id` (`requireRole(Role.ADMIN)`, same gate as GET/POST/PATCH) soft-deletes via `User.deletedAt` (sets it, doesn't remove the row) and deletes all the target's `Session` rows in the same `$transaction`. 404 `{message:"User not found"}` if the id doesn't exist OR `deletedAt` is already set (confirmed: deleting the same disposable user twice → 204 then 404, proving the soft-delete is treated as "gone" for all read/delete purposes despite the row persisting). 403 `{message:"Admin users cannot be deleted"}` if the target's role is ADMIN, checked server-side independent of the UI's disabled button — verified by calling the seeded ADMIN's own id directly via `request.delete` (looked the id up via the existing `GET /api/users` listing pattern, same as other admin-lookup tests in this suite). A soft-deleted user's subsequent sign-in attempt hits the existing `hooks.before` check in `server/src/auth.ts` (already covered by [[project-helpdesk-auth-e2e]]'s sign-in tests for the wrong-password/unknown-email cases) and gets the same generic "Invalid email or password" — didn't re-assert the exact wording for the post-delete case, just asserted a 4xx status, since the message is intentionally indistinguishable from other auth failures by design.

Test layout: added a `delete user dialog` describe block to `e2e/ui.spec.ts` (sibling to `edit user dialog`) with 3 tests — cancel (opens alertdialog with the name interpolated into the description text, Cancel closes it, asserts via a `page.route` DELETE-flag that no request fired, row still visible), confirm (creates a disposable user via `page.request.post`, captures the `{n} total` count before using the same `waitForResponse`-before-`login` pattern as prior count-flake fixes, clicks Delete, asserts row gone + count decremented by exactly 1 + an API-level sign-in attempt with the original password now fails), and a pure-state check that the seeded Admin row's delete button `toBeDisabled()` (deliberately did NOT click it — asserting the disabled attribute is sufficient proof it can't be triggered from the UI, clicking a disabled button proves nothing extra). Added `DELETE /api/users/:id authorization` (401/403/204 matrix, mirroring the existing PATCH block's `createDisposableUser` helper shape exactly but as its own copy rather than a shared import — this suite's existing convention is one local helper per describe block, not a shared test-utils module) and a separate `DELETE /api/users/:id behavior` block in `e2e/users-api.spec.ts` with 4 focused tests (admin-can't-delete-admin 403, double-delete 404, nonexistent-id 404, soft-deleted-user-cant-sign-in) — kept these as 4 separate tests rather than consolidating further since each asserts a genuinely distinct status/message combination and combining them would have made failures harder to attribute to a specific cause.

Confirmed `response.status()` reports `204` directly (not e.g. `200`) for the empty-body success case, and Playwright's `APIResponse.ok()` is `true` for it — no surprises there, matched the task's expectation exactly on first try.

Final count after this work: 51 e2e tests total (41 pre-existing + 3 new in `ui.spec.ts` + 7 new in `users-api.spec.ts`). Confirmed stable across 2 consecutive full `npx playwright test` runs from the repo root (51/51 both times), no flake-fix iteration needed this time.

## Dedicated happy-path CRUD spec: e2e/user-management.spec.ts (2026-06-25)
Per explicit user request, added a standalone `e2e/user-management.spec.ts` with exactly one test that reads as a single continuous admin workflow (create -> read/verify -> update -> delete) against one disposable user, deliberately separate from `e2e/ui.spec.ts`'s validation/error-heavy `add/edit/delete user dialog` blocks and `e2e/users-api.spec.ts`'s authorization matrix. Explicitly told not to duplicate those — this file only proves the happy path, end to end, in one place.

Implementation notes:
- Reused every convention from [[project-helpdesk-auth-e2e]]'s prior add/edit/delete work verbatim: `waitForResponse` registered before `login()` for the `{n} total` count flake-fix, `dialog.getByLabel(...)` scoped to the open `Dialog`/`alertdialog` (never bare `page.getByLabel`), exact-string `{ name: \`Edit ${name}\`, exact: true }` / `{ name: \`Delete ${name}\`, exact: true }` button matches (never a regex), and avoided the substring "Name" in the post-edit user name (used `E2E Mgmt Updated ${Date.now()}`).
- For the "Read" step, asserted real column-cell values via `row.getByRole('cell').nth(0)` (Name) / `.nth(1)` (Email) rather than just `toContainText`, since the task asked to verify "correct data in the right columns" specifically — `nth()` on `getByRole('cell')` within a row-scoped locator is reliable because column order (Name, Email, Role, Created, Actions) is fixed and asserted elsewhere in `ui.spec.ts`'s `users page` test. For the Created column (`.nth(3)`), asserted `not.toBeEmpty()` rather than an exact date string, since `UsersPage.tsx` renders it via `new Date(user.createdAt).toLocaleDateString()` (`client/src/pages/UsersPage.tsx` line 290) — locale/timezone-dependent formatting makes an exact string fragile; non-empty is sufficient proof the column renders real data.
- Asserted the total count stays unchanged across the update step (still `beforeCount + 1`, not incremented again) as a deliberate extra check that update doesn't accidentally create a row — cheap to add since the locator was already in scope.
- Passed on the very first run, and stayed stable across 2 consecutive full `npx playwright test` runs from repo root (52/52 both times: 51 pre-existing + this 1 new test). No flake-fix iteration needed.

## Inbound email -> ticket ingestion: e2e/inbound-email.spec.ts (2026-06-25)
New feature: `POST /api/email/inbound` (`server/src/index.ts`) converts inbound support emails into tickets/replies. Added a standalone `e2e/inbound-email.spec.ts` (6 new tests; full suite went 52 -> 58, stable across 2 consecutive full `npx playwright test` runs, 58/58 both times, no flake-fix iteration needed). Note: a `e2e/tickets.spec.ts` (1 test, plain `<li>`/`getByRole('listitem')` ticket-creation happy path) had appeared in the suite since my last update and wasn't yet documented here — folded it in now. Baseline before this task was confirmed at 52/52 by an actual run, matching what the task described.

Key facts about this endpoint, confirmed by reading the route/middleware/schema directly (not guessed):
- Auth is `requireInboundSecret` (`server/src/middleware.ts`) — a flat header-equality check against `process.env.INBOUND_EMAIL_SECRET`, totally separate from session-based `requireAuth`/`requireRole`. No sign-in needed to call it; used the bare `request` fixture directly with `headers: { 'X-Inbound-Secret': ... }`.
- Chose to **hardcode** the literal `'test-inbound-secret'` as a `const INBOUND_SECRET` in the spec file (with a comment noting it must match `server/.env.test`'s `INBOUND_EMAIL_SECRET`) rather than parsing the `.env.test` file or adding `dotenv` — it's a fixed test-fixture value, not a real secret, and a parsing dependency felt like overkill for one constant. No pushback expected either way per the task, but this is the convention now if the same question comes up again for other secret-gated test-only endpoints.
- **Real gotcha hit on first run:** the task suggested testing a "missing subject" by omitting the `subject` key entirely. That actually fails *before* reaching the intended `.min(1, "Subject is required")` zod message — `z.string()`'s base type check runs first and rejects `undefined` with `"Invalid input: expected string, received undefined"`. Fixed by sending `subject: ''` (a valid string that's empty) instead of omitting the key, which is what actually exercises the `.min(1)` message. General lesson: when a zod schema chains `.string().min(1, "X is required")`, "required" in the test sense means *empty string*, not *absent key* — they hit different error messages. Worth checking this for any future required-string-field validation test in this codebase rather than assuming omission and empty-string are equivalent.
- The create -> reply -> replay flow (steps sharing `messageId`/`ticketId` across phases) was written as **one test function** with three inline phases (plain `const`/`let` variables carried forward, comments marking "Phase 1/2/3"), not three separate `test()`s in a `describe`. Rationale: Playwright's declaration-order guarantee across separate tests felt like more moving parts than necessary for a flow that's fundamentally one continuous scenario with tightly coupled state at every step — matches how `user-management.spec.ts` made the same call for its CRUD happy path. Confirmed `replayResponse` body deep-equals `{ ticketId, threaded: false, deduplicated: true }` exactly (used `toEqual`, not `toMatchObject`) since `IngestResult`'s shape is small and fully known.
- `GET /api/tickets` (existing endpoint, `requireAuth` only, either role) now also returns `requesterEmail: string | null` and `messages: {id, fromEmail, body, messageId, createdAt}[]` per ticket (ordered by message `id asc`). Manually-created tickets (the pre-existing "Add ticket" UI flow) have `requesterEmail: null, messages: []` — untouched by this feature, confirmed not to need any test changes to `tickets.spec.ts`.
- UI (`client/src/pages/Home.tsx`): the ticket list is **real `<li>`/`getByRole('listitem')`** markup (confirmed via `tickets.spec.ts`'s pre-existing pattern), not a bare unstyled div as a first read of the task's prose suggested — so `page.getByRole('listitem').filter({ hasText: subject })` for row-scoping worked directly, no `getByText`-based container lookup needed. Inside that scope: the `via email · <email>` text is a plain `<span>` (`getByText` required, not a role query — consistent with this codebase's general plain-div/span convention for non-interactive shadcn-derived text), the toggle is a real `<button>` with exact accessible name `` `Show ${n} ${n===1?'message':'messages'}` `` flipping to `Hide ...` after click (`getByRole('button', {name: ..., exact: true})` — exact match used throughout this suite for icon/label buttons, see the Edit/Delete button gotcha entries above), and each expanded message renders as literal text `` `${fromEmail}: ${body}` `` inside an `<li>`, matched via scoped `getByText` using a template string identical to the render.
- Didn't need the `waitForResponse`-before-`login` count flake-fix pattern here: every assertion in this file is either pure `request`-fixture API calls (no UI count display involved) or scoped to a specific ticket's `<li>` by its unique subject (not a `{n} total` count), so the documented flake mode above never applies to this file's assertions.

## Subject-fallback threading test added; reopening-via-reply test skipped with documented reason (2026-06-25)
Extended `e2e/inbound-email.spec.ts` (no new file) with one more test for `ingestInboundEmail`'s (`server/src/inboundEmail.ts`) second matching branch: a reply with **no** `messageId`/`inReplyTo`/`references` at all still threads onto an existing ticket purely via same `requesterEmail` + `normalizeSubject()` match (case-insensitive, `Re:`/`Fwd:`/`FW:` prefix stripped). This branch was previously only proven against a mocked DB in `server/src/inboundEmail.test.ts` (Bun unit test), not e2e. Test: POST fresh email (no messageId) -> 201; POST second email, same sender, `Re: ${subject.toLowerCase()}`, also no messageId/inReplyTo -> expect `200 { threaded: true, deduplicated: false }` with the same `ticketId`, then confirm via `GET /api/tickets` filtered by `requesterEmail` that there's exactly one ticket with 2 messages. Suite went 58 -> 59, stable across 2 consecutive full `npx playwright test` runs (59/59 both times), no flake-fix iteration needed.

**Deliberately skipped (documented in-file, not silently dropped):** proving the threaded-reply-reopens-a-closed-ticket behavior (`prisma.ticket.update({ data: { status: "open" } })` on the threaded path, `inboundEmail.ts` lines 69-74) end-to-end. There is **no API/UI path anywhere in this app that ever sets a ticket's status to `"closed"`** — confirmed by reading every route in `server/src/index.ts` (only `GET`/`POST /api/tickets` exist; neither touches `status`) — so e2e setup would require a direct DB write, and I confirmed this isn't a one-line addition in this project's current state:
- No `e2e/package.json` — `e2e/` is plain files under the root workspace, run by `playwright test` (Node), not Bun.
- No root `tsconfig.json` and no wiring to the `server` workspace's TS config.
- No `dotenv` (or any env-loading) at the repo root — `server/.env.test`'s `DATABASE_URL` isn't available to the Playwright process at all (`playwright.config.ts`'s `webServer` entries pass `--env-file=.env.test` to the *server* subprocess only, not to the Playwright test runner itself).
- `server/src/db.ts`'s `PrismaClient` is generated to a Bun-resolved relative path (`server/generated/prisma`) and depends on `@prisma/adapter-pg` — neither installed/resolvable from the root/e2e context without adding new dependencies.
- No `psql` on this machine's PATH either, ruling out a raw-SQL fallback without Prisma.

Concluded this is genuinely option-(b)-territory (skip + explain), not option-(a) (the task itself suggested trying (a) first, in case it really was a one-liner — it isn't, here). Left a long comment block in `e2e/inbound-email.spec.ts` (right after the subject-fallback test) spelling out the above and proposing two concrete unblocks if this coverage is ever wanted: (1) a guarded test-only endpoint to set ticket status (same secret-header pattern as `requireInboundSecret`), or (2) wiring a real `DATABASE_URL`-based Prisma/pg client into the e2e workspace (new dependency + env loading at the repo root). The existing mocked-DB unit test (`server/src/inboundEmail.test.ts`) remains the only coverage for this specific branch until one of those exists. **General lesson for future "needs direct DB write from e2e" requests in this repo: the answer is no, not yet — this is a real, confirmed infrastructure gap, not laziness. Don't re-investigate from scratch each time; point to this entry and the in-file comment.**

**UPDATE 2026-06-25: this gap is now closed.** The user explicitly asked for option (2) above to be built, and it now exists — see the entry immediately below. The "answer is no, not yet" lesson no longer applies; direct DB access from e2e specs is now a supported, working pattern in this repo.

## Direct DB access from e2e specs: e2e/db.ts (2026-06-25)
Added `e2e/db.ts`, a thin Prisma client for use directly from Playwright specs, unblocking the gap documented just above. Two infra pieces had to land first (done in a prior session, confirmed still in place): `"@prisma/adapter-pg": "latest"` added to the **root** `package.json`'s devDependencies (`bun install` run), and confirmed Node 24's built-in `process.loadEnvFile` is available so no `dotenv` dependency was needed.

`e2e/db.ts` mirrors `server/src/db.ts`'s adapter pattern exactly (`PrismaPg` + `PrismaClient` from the generated client), but adds one line `server/src/db.ts` doesn't need: `process.loadEnvFile(path.resolve(__dirname, '../server/.env.test'))` at module load, because `playwright.config.ts`'s `webServer` entries only pass `--env-file=.env.test` to the *spawned server subprocess* — the Playwright test-runner process executing spec files never sees `DATABASE_URL` otherwise. Loads the whole file (simpler than picking out just one var; harmless to load the rest).

**The one genuinely unverified piece going in — confirmed working, no issues hit:** `server/generated/prisma/client.ts` is Prisma's newer TS-first generator (`provider = "prisma-client"`) output, an ESM `.ts` file that internally imports siblings with explicit `.ts` extensions (e.g. `from "./enums.ts"`) and uses `import.meta.url`. This already worked under Bun (imported from `server/src/*.ts`), but whether Playwright's esbuild-based TS transform would resolve those same explicit-`.ts`-extension relative imports when the *importing* file lives outside `server/` (in `e2e/db.ts`) was untested. Proved it with a throwaway smoke spec (`prisma.ticket.count()`) before touching real spec files — passed first try, no transform issues, no special config needed in `playwright.config.ts`. Deleted the smoke spec immediately after confirming.

Used this to finally write the previously-skipped test: `e2e/inbound-email.spec.ts`'s new `POST /api/email/inbound reopens a closed ticket` describe block creates a ticket via the webhook, force-closes it with `await prisma.ticket.update({ where: { id: ticketId }, data: { status: 'closed' } })`, sends a reply with `inReplyTo` matching the original `messageId`, and asserts via `GET /api/tickets` that `status` is back to `'open'`. Confirmed by reading `server/src/inboundEmail.ts` first that this is the right path to exercise: the `inReplyTo`/`references` branch matches via `ticketMessage.findFirst` with **no status filter at all** (unlike the subject-fallback branch, which filters `status: { not: "closed" }` and so can never match a closed ticket by design) — so a closed ticket is only reachable again via the `inReplyTo`/`references` reference-id path, never via subject-fallback. Removed the long skip-comment block that previously documented this gap (now factually wrong) and replaced it with a short comment pointing at why the real DB write is necessary here.

Added `test.afterAll(async () => { await prisma.$disconnect() })` at the bottom of `e2e/inbound-email.spec.ts` (the only spec file using `prisma` directly) so the Playwright process doesn't hang on an open DB connection post-run — confirmed the full suite run exits cleanly without hanging.

Suite went 59 -> 60. Confirmed stable across 2 consecutive full `npx playwright test` runs from the repo root (60/60 both times), no flake-fix iteration needed. **This pattern (`import { prisma } from './db'` + direct Prisma calls + `afterAll` disconnect) is now the established way to do direct-DB-write e2e setup in this repo — reuse `e2e/db.ts` as-is for any future spec that needs to force backend state no UI/API path can reach, rather than re-investigating feasibility.**

## Ticket list ordering flips to newest-first; added a regression test (2026-06-25)
`GET /api/tickets` (`server/src/index.ts`) changed `orderBy: { id: "asc" }` -> `orderBy: { id: "desc" }`. The `messages` include is still `orderBy: { id: "asc" }` (unchanged, chronological thread order within a ticket — don't confuse the two when reading/writing tests here).

Added one test to the existing `ticket form` describe block in `e2e/ui.spec.ts` (sibling to "creating a ticket increments the total count by one"): create two tickets in sequence with unique subjects (`e2e order check A ${Date.now()}` / `...B...`), then assert B's `<li>` precedes A's `<li>` using the exact same `el.parentElement?.children` index-comparison technique already established by the `users page` describe block's Admin-before-Agent sort assertion (see entry above) — reused verbatim, just applied to `getByRole('listitem')` instead of `getByRole('row')`. Confirmed via `Home.tsx` that tickets render as a flat `<ul>` of `<li>` (all siblings under one parent), so the technique transfers directly with no adjustment. No new flake-fix iteration needed — passed first try, confirmed stable across 2 consecutive full `npx playwright test` runs from the repo root (61/61 both times: 60 pre-existing + this 1 new test).

## New Tickets page (/tickets) + e2e/tickets-page.spec.ts (2026-06-25)
New route `/tickets` (`client/src/pages/TicketsPage.tsx`), added to `App.tsx`'s `<Routes>` with **no role gate** at all (unlike `/user`, which redirects non-admins) — matches backend reality exactly: `GET /api/tickets` (`server/src/index.ts`) has always used plain `requireAuth`, never `requireRole(Role.ADMIN)`. `NavBar.tsx` renders a "Tickets" link for any logged-in user (ADMIN or AGENT), placed before the admin-only "Users" link. This is the first page in the app where an AGENT has full, unrestricted access — worth remembering as a contrast case to `authorization.spec.ts`'s AGENT-blocked-from-`/user` test.

Page mirrors `UsersPage.tsx`'s shadcn `Card`/`Table` structure exactly (loading skeleton / `isError` "Failed to load tickets." / empty "No tickets yet." states), fetched via `useQuery(['tickets'], ...)` (TanStack Query, per this repo's `CLAUDE.md` convention — confirmed `TicketsPage.tsx` does NOT use the older `useEffect`+`useState` pattern that `Home.tsx`/`UsersPage.tsx` predate). Columns: Status (`Badge`, `default`/`open` or `secondary`/`closed`) | Subject (`#<id>` muted chip + text, same chip styling as Home's ticket `<li>`s) | Category (new `Ticket.category` field, Prisma enum `BUG|FEATURE|QUESTION|OTHER`, defaults `OTHER`; rendered `Badge variant="outline"`, literal enum text — nothing in the UI sets it yet, so every ticket is `OTHER` in practice) | Sender (`ticket.requesterEmail ?? '—'`, real em dash character) | Created (new `Ticket.createdAt` field, `@default(now())`, rendered via `toLocaleDateString()` like the Users table's Created column). Read-only page, no create/edit/delete actions — ticket creation is still only the Home page's "New ticket" form or the inbound-email webhook.

Added `e2e/tickets-page.spec.ts` (4 tests, suite went 61 -> 65, confirmed stable across 2 consecutive full `npx playwright test` runs, 65/65 both times, no flake-fix iteration needed, passed first try): (1) admin nav-link-click lands on `/tickets`, asserts column headers via `getByRole('columnheader')` + `toHaveText([...])` (same pattern as the Users table's column-lock-in test); (2) AGENT sees the link and loads the page with no redirect (the explicit regression guard for the role-gate difference above); (3) a manually-created ticket's row shows `open` badge, subject, `OTHER` category badge, and an em-dash sender cell (`row.getByRole('cell').nth(3)`), plus a non-empty Created cell (`.nth(4)`, same locale-fragility reasoning as `user-management.spec.ts`'s Created-column check); (4) an inbound-email-created ticket's row shows the real sender email in that same Sender cell instead of an em dash. Reused the `INBOUND_SECRET = 'test-inbound-secret'` literal as a local copy (not imported from `inbound-email.spec.ts`) — this suite's convention is one local helper/constant per file, not a shared test-utils module (see the Delete-user entry above for the same call made explicitly).

No new gotchas hit — `getByText('open'|'OTHER', { exact: true })` scoped to the row locator was unambiguous on first try (no other row text collided), and `getByRole('cell').nth(n)` for Sender/Created followed the exact same convention already established for the Users table (Name/Email cell-by-index) and `user-management.spec.ts`.

## Search/sort/filter controls added to /tickets; extended tickets-page.spec.ts (2026-06-25)
`TicketsPage.tsx` gained client-side search (`Input` placeholder "Search subject or sender", matches `subject + ' ' + requesterEmail` lowercased substring), two shadcn `Select` filters (status: ALL/open/closed; category: ALL/BUG/FEATURE/QUESTION/OTHER), and sortable column headers (`SortableHead` wraps a real `<button type="button">` per `TableHead`, default sort `{key: 'createdAt', direction: 'desc'}`) -- all operating on the already-fetched array via one `useMemo`, no new API calls. Added a new `tickets page search and filters` describe block (4 tests: search-by-subject, search-by-sender-email, closed-status-filter empty state, BUG-category-filter empty state) and `tickets page sorting` describe block (2 tests: Subject-column click-to-sort-asc-then-desc via the established `el.parentElement?.children` index-comparison technique, and a default-newest-first-sort regression test) to the existing `e2e/tickets-page.spec.ts`, sibling to the pre-existing `navigation` and `tickets page row content` blocks (left untouched). Suite went 65 -> 71. Disambiguated the two `Select` triggers (neither has a distinguishing `aria-label`; accessible name comes only from rendered `SelectValue` text) via `page.getByRole('combobox').filter({ hasText: 'All statuses' })` / `{ hasText: 'All categories' }` -- confirmed working, no ambiguity hit. `SortableHead`'s button matched via `page.getByRole('columnheader', { name: 'Subject' }).getByRole('button')`.

**Real gotcha hit and fixed (sandbox clock drift across sessions, not an app bug):** the "default sort is newest-first" test initially asserted the new ticket was the literal first `<tr>` in `tbody` (`page.locator('tbody tr').first()`). This failed consistently against a *real* environment artifact: a batch of ~80 pre-existing tickets in the never-truncated `helpdesk_test` DB, all created in an earlier session, share a `createdAt` of `2026-06-25T12:14:52.713Z` -- which is **later** than actual current time in a subsequent session (confirmed directly via `prisma.ticket.aggregate({_max: {createdAt: true}})` and comparing to `date -u`: that batch's timestamp was ~1.5 hours ahead of "real now" in the next session). This means the sandbox's system clock is not guaranteed monotonic *across* separate Playwright/session invocations, even though `Date.now()` is obviously monotonic *within* one process's lifetime. Any test asserting "the row I just created is the global first row by createdAt desc" is at risk of this exact flake whenever it runs in a session where the clock reads earlier than some prior session's clock did.

Fix applied: don't assert global index 0 against the whole accumulated table. Instead, use `e2e/db.ts` to insert a sibling ticket directly with a hardcoded far-past `createdAt` (`new Date('2000-01-01T00:00:00.000Z')`) as a fixture fully under the test's control, then assert only that the new UI-created ticket's row precedes *that* sibling's row (relative `el.parentElement?.children` index comparison, same technique as the Subject-sort test). This proves real `createdAt desc` sorting behavior deterministically without depending on the absolute clock state of unrelated historical rows, and without mutating/cleaning up that historical contamination (which would alter shared fixture data other specs might incidentally depend on -- consistent with this memory's established stance on never truncating/rewriting other specs' rows). Left an in-file comment explaining why a literal index-0 claim was deliberately not made. Confirmed stable across 2 consecutive full `npx playwright test` runs from the repo root (71/71 both times) after the fix; the literal-index-0 version failed deterministically every time before it.

**General lesson for this sandbox specifically:** if a future test needs to prove "this is the newest/oldest by `createdAt`" against the *entire* accumulated, never-truncated table (not just a relative comparison between two fixtures the test controls), check current real time (`date -u` via Bash, or `new Date()` in a debug Prisma query) against `MAX(createdAt)`/`MIN(createdAt)` in the table first -- don't assume monotonic wall-clock time across sessions in this environment. The relative-comparison-via-controlled-fixture pattern (using `e2e/db.ts` to force a known-old or known-new sibling) is the reusable workaround; reach for it before spending time debugging what looks like a sort-logic bug but is actually environment clock drift.

Also fixed a more ordinary race while building these tests: the default-sort test originally waited only for the create-ticket `<li>` to appear on Home before navigating to `/tickets`, relying on the `ticketsLoadedOnPage` GET-response wait registered after `page.goto`. This wasn't sufficient because Home's add-ticket mutation (`useMutation` `onSuccess`) invalidates and refetches `['tickets']` in the background on the *same* page right after the POST resolves -- so a GET observed shortly after could be that background refetch (already in flight before navigation) rather than the GET triggered by `/tickets` mounting fresh. Fixed by waiting for the POST response itself (`res.request().method() === 'POST'`) before navigating, guaranteeing the write landed server-side first. This pattern (wait for the mutating request's own response, not just its UI side-effect, when the next step is a navigation that could race against a background-invalidation refetch) is worth reusing for any future test that creates data via one page and immediately navigates to verify it on another.

## Useful verification technique
When unsure of exact server-rendered error text instead of guessing from minified `node_modules` dist files (hard to grep, code is bundled/minified), just start the test server directly (`cd server && bun --env-file=.env.test run dev`) and `curl` the endpoint directly with `Origin: http://localhost:4173` header (required — better-auth's trustedOrigins check rejects requests without it). Confirmed this matches what the same DB user's real seeded values exercise in playwright. Remember to kill the background process afterward (`netstat -ano | grep :3002` → `taskkill //F //PID <pid>` on Windows/git-bash).

## TicketsPage redesign: read-only Table → per-row status Select + Add ticket Dialog (2026-07-07)
`client/src/pages/TicketsPage.tsx` changed significantly from the original read-only table:

**Status column**: Now a per-row `<Select>` (Radix, role=`combobox`) with `aria-label="Status for {ticket.subject}"` on the `SelectTrigger` — NOT the old `Badge`. To assert a ticket's status use `row.getByRole('combobox', { name: 'Status for {subject}' })` with `toHaveText('open')`, NOT `row.getByText('open', { exact: true })` (the latter still works since `SelectValue` renders text, but the former is more precise and expressive). The old `getByText('open', { exact: true })` was not fixed in the pre-existing test, meaning it still passes incidentally — but new tests should prefer the combobox locator.

**ticketStatuses** is now `["open", "resolved", "closed"]` (3 values, not 2). `closed` status can now be set via the UI's per-row Select, but no existing e2e test ever exercises this (the `filtering by closed status` empty-state test relies on no closed tickets existing — latent, but currently safe).

**Sender cell**: Now shows dual-field (name + email as separate `<span>`s in a `flex-col` div) if either is set, or `'—'` if both are null. Logic confirmed in JSX: `{ticket.requesterName || ticket.requesterEmail ? <div>...</div> : '—'}`. Name in `<span class="text-foreground">`, email in `<span class="text-xs text-muted-foreground">`. Assert with `toContainText(name)` + `toContainText(email)` on the row, NOT `toHaveText` exact-match on the Sender cell (`.nth(3)`).

**Add ticket Dialog**: `Button` in `CardHeader`/`CardAction` opens a Radix `Dialog` (`role="dialog"`, same pattern as UsersPage add/edit). Fields: `#ticket-subject` (zod message "Subject is required"), `#ticket-category` (Controller-wrapped Select, defaults `OTHER`, options BUG/FEATURE/QUESTION/OTHER), `#ticket-requester-name` optional, `#ticket-requester-email` optional with zod message "Enter a valid email" for non-empty malformed (`.refine(v => v === "" || ...email().safeParse...`). Submit button: "Create ticket" / "Creating…" (real ellipsis) when pending. On success: invalidates `['tickets']`, resets form, closes dialog. To open category Select: `dialog.locator('#ticket-category').click()`, then `page.getByRole('option', { name: 'BUG', exact: true }).click()` (options render in portal outside dialog scope). Use `dialog.getByLabel('Sender name (optional)')` / `'Sender email (optional)'` for those fields (exact label text as written in the JSX `FieldLabel`).

**Test added in `tickets page search and filters`**: The "BUG-category-filter shows empty state" test was updated to combine the category filter with a unique search term (`Date.now()`-suffixed subject) rather than relying on "no BUG tickets exist in the table" -- the new add-ticket dialog test creates a real BUG-category ticket on every run into the never-truncated DB, permanently invalidating the bare BUG-filter empty-state assumption. Going forward, any empty-state filter test that relies on global absence (rather than a controlled-fixture scope) is fragile to this same pattern.

## waitForResponse navigation race class — definitive fix pattern (2026-07-07)
Root cause proven (not just theoretical): In this app, `login()` navigates to `/` (Home), which fires Home's own `GET /api/tickets` immediately. Any subsequent `waitForResponse` registered for GET `/api/tickets` that is registered *while that earlier request is still in-flight* risks resolving on Home's response rather than the one triggered by the test's own navigation (`page.goto('/tickets')` or a `.click()` on the Tickets nav link). The race becomes more likely as the never-truncated test DB accumulates rows (making each GET response slower), and it reliably manifested once the DB reached ~440 rows.

**The only safe ordering** for any sequence `Home-GET → navigation → target-page-GET`:

1. Register `waitForResponse` for Home's own GET **before `login()`** (so it's guaranteed to catch that exact request), then `await` it after `login()` returns. This ensures the slot is consumed and no pending Home request is left.
2. Register a fresh `waitForResponse` for the target page's GET **after** step 1's await completes (safe: no competing request left in-flight).
3. Trigger the navigation (`goto` or `click`), then `await` the step 2 wait.

For tests that create tickets via Home before navigating:
- Register waits for **both** the POST **and** the background refetch GET that the mutation's `onSuccess` invalidation triggers, **before clicking Add ticket** (not after — registering after risks missing a refetch that already resolved, which hangs forever on `waitForResponse`). Await both in sequence (POST first, then GET). Only then register a new wait for the target-page GET, then navigate.
- The "let `{n} total` text appear" approach is NOT a valid settle signal: `tickets` defaults to `[]`, so `{0} total` renders immediately before any fetch resolves, making `expect(page.getByText(/total$/)).toBeVisible()` always trivially true.

**All tests in `e2e/tickets-page.spec.ts` now use this corrected pattern.** `e2e/ui.spec.ts` was confirmed safe: all its `waitForResponse` calls target `/api/users`, and `Home.tsx` (where `login()` lands) never calls `/api/users`, so there's no competing earlier request for those waits regardless of registration timing relative to `login()`. Suite count after this work: 75 tests (14 in `tickets-page.spec.ts`, 61 elsewhere), stable 75/75 across 2 consecutive full `npx playwright test` runs.
